apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-5
  namespace: microapps
  labels:
    app: app-5
    type: py-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-5
  template:
    metadata:
      labels:
        app: app-5
    spec:
      serviceAccountName: microapps
      containers:
      - name: app-5
                image: arunxim/app-5:commit-f21bb6cd
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "96Mi"
            cpu: "100m"
          limits:
            memory: "192Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: app-5
  namespace: microapps
  labels:
    app: app-5
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: app-5
