apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: argocd-application-controller   # ← same name!
spec:
  template:
    spec:
      containers:
      - name: argocd-application-controller
        args:
        - --sharding-method
        - round-robin   # example different arg for "this view"